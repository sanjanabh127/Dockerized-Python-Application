# Dockerized Python Application

A simple Python application containerized using Docker.
This project demonstrates writing a Dockerfile and building a Docker image locally.
The container runs the Python script in an isolated environment.
Built as a beginner-friendly Docker deployment project.
